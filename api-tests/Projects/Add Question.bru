meta {
  name: Add Question
  type: http
  seq: 4
}

post {
  url: {{baseUrl}}/api/projects/{{projectId}}/questions
  body: json
  auth: none
}

body:json {
  {
    "question": "What problem are we trying to solve?"
  }
}

assert {
  res.status: eq 201
}
